package]
name = "{{ project_name }}-fuzz"
version = "0.1.0"
authors = ["Automatically generated"]
publish = false
edition = "2018"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"

# Uncomment the correct repo and delete the other comments
#
# [dependencies.REPLACE_WITH_NAME_OF_PACKAGE]
# path = ".."
# 
# TODO: Add instructions in case of workspace
# TODO: Add instructions in case of feature

# Prevent this from interfering with workspaces
[workspace]
members = ["."]

[[bin]]
name = "insert"
path = "fuzz_targets/insert.rs"
test = false
doc = false

